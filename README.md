# StreamBuild documentation

Mintlify documentation for [StreamBuild](https://github.com/chio-labs/streambuild).

## Local preview

From this repository:

```bash
npx mintlify dev
```

The product source and executable examples live in the StreamBuild repository. Documentation
examples should match the current `stb` CLI and checked-in project formats before publication.
